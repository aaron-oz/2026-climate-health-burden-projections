#+TITLE: Draft email to Caspar: pilot accepted, go to stage 2
#+DATE: 2026-09-14

Draft for Aaron's review; NOT sent. Merge commit on main: f48cea9 (the
=tmrel-keep-17-causes= branch). Supersedes the earlier
re-pilot draft (the team decided on 2026-09-14 to keep all 17 causes, so
there is no re-pilot). The review note is in the repo at
=docs/reviews/ssp245-pilot-review-2026-09-14.org=.

-----

Subject: Re: ssp245 rerun: pilot accepted (Haiti asterisked), please start the full grid

Hi Caspar,

Following up on today's call. Your checker's FAIL was right and stopping
was the correct call, but as we discussed the failure is confined to
Haiti and we are keeping the method as it is, so the pilot you already
ran is the configuration we go forward with. No re-pilot.

What the pilot showed, for the record:

- 14 of the 15 locations match our predictions within 0.7 % (Nicaragua
  3.6 %), the negative draws are gone, and the interval widths sit at
  0.8 to 1.7 times GBD 2019's.
- Haiti: in 22 of its 500 draws the IHME forecast for inj_disaster
  (exposure to forces of nature) is earthquake-scale, 187,000 to 305,000
  deaths, roughly the 2010 event (for comparison, GBD 2023's
  retrospective estimate of Haiti's forces-of-nature deaths in 2022
  itself is 9, so the tail is a feature of the forecast, not of the
  year). Disaster is then 81 % of those draws' cause mix, the
  death-weighted risk curve bottoms out at the floor of the TMREL search
  range (6.6 C), and every cause in those draws gets a heat PAF near 1.
  Nothing was wrong on your side: re-deriving Haiti's TMRELs here from
  your cache reproduces your values exactly. IHME's own released TMRELs
  for Haiti collapse the same way in 2010. The team's decision is to keep
  all 17 causes in the TMREL weights everywhere, as Burkart and IHME do,
  and to asterisk Haiti in the outputs.

Re-running the checker here with Haiti set aside gives WARN (Nicaragua
only) and clears stage 2. Two small things changed on main that you need
before the summarizer: the summary tables gain a draw median beside every
mean plus a shock-tail flag in the QA report (this is what the Haiti
asterisk rests on), and the checker gained a --exempt option. The
pipeline code you ran the pilot with is unchanged in effect; your
derived-TMREL cache files stay valid and will be reused.

Steps in order, all from the repo root:

1. Update the repo and confirm you have the merge:

   git pull
   git log --oneline -1 f48cea9

   The second command should print the merge commit. If it errors with
   "unknown revision", stop and email me.

2. The full burden pass, your original command shape plus the flag:

   parallel -j 125 \
     'Rscript global-scripts/util_run_global.R --location_id={} --scenarios=ssp245 --years=2022-2050 --force_burden=TRUE' \
     ::: $(Rscript -e 'cat(readRDS("output/intermediate/ihme_loc_map.rds")$loc_id)')

   Temperature conversions are reused; only the burden phase runs. This
   recomputes the pilot combos' remaining years and sweeps up the three
   stale combos from the August 14 check. Scaling your 45-minute pilot
   linearly gives on the order of a day and a half at -j 125; we do not
   know how well the parallel efficiency holds at that width, so please
   note the actual wall-clock.

3. The summarizer, with --force this time (the per-location cache under
   output/summary/_cache/ would otherwise serve the old columns):

   Rscript global-scripts/util_summarize_run.R --scenarios=ssp245 --jobs=16 --force

4. The review-bundle export, exactly as before:

   Rscript global-scripts/util_export_review_bundle.R --scenarios=ssp245 --jobs=16

5. Email us the bundle, plus the wall-clock for steps 2 and 3.

Optional, only if we decide on the call follow-up to give Haiti a
16-cause run rather than a footnote (do NOT run this unless we confirm;
it overwrites Haiti's 17-cause output, and the summarizer would then mix
the two configurations without saying so):

   Rscript global-scripts/util_run_global.R --location_id=114 --scenarios=ssp245 --years=2022-2050 --force_burden=TRUE --tmrel_weight_exclude=inj_disaster

   followed by steps 3 and 4 again.

Thanks!
Aaron
